# Solana RPC Configuration
# Get your RPC endpoints from a provider like Chainstack, Helius, QuickNode, etc.
SOLANA_NODE_RPC_ENDPOINT=https://your-rpc-endpoint.com
SOLANA_NODE_WSS_ENDPOINT=wss://your-wss-endpoint.com

# Solana Wallet Private Key
# IMPORTANT: Keep this secret! Never commit this file to version control.
# Format: Base58 encoded private key (array format like [1,2,3,...] or base58 string)
SOLANA_PRIVATE_KEY=your_private_key_here

# Geyser Configuration (Optional - for fastest token detection)
# Only needed if using geyser listener_type in bot config
GEYSER_ENDPOINT=your_geyser_endpoint_here
GEYSER_API_TOKEN=your_geyser_api_token_here

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_KEY=your_api_key_here

# Security
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60
